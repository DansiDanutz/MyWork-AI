---
phase: 01-foundation-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - package.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/modules/.gitkeep
  - src/shared/components/.gitkeep
  - src/shared/lib/.gitkeep
  - src/shared/types/.gitkeep

autonomous: true

must_haves:
  truths:

```markdown

- "Next.js development server starts without errors"
- "TypeScript compilation succeeds with strict mode"
- "Project uses App Router architecture with src/ directory"
- "Module directories exist for feature-based organization"

```yaml

  artifacts:

```yaml

- path: "package.json"

  provides: "Project dependencies and scripts"
  contains: "next"

- path: "tsconfig.json"

  provides: "TypeScript configuration with strict mode"
  contains: "strict"

- path: "src/app/layout.tsx"

  provides: "Root layout component"
  contains: "RootLayout"

- path: "src/modules/.gitkeep"

  provides: "Modules directory for feature-based architecture"

- path: "src/shared/lib/.gitkeep"

  provides: "Shared library directory"

```

  key_links:

```yaml

- from: "package.json"

  to: "npm run dev"
  via: "scripts.dev"
  pattern: "next dev"

- from: "tsconfig.json"

  to: "src/*"
  via: "paths alias"
  pattern: "@/\\*"

```html

---

<objective>
Initialize a Next.js 15+ application with TypeScript and configure the modular
project structure for feature-based development.

Purpose: Establish the foundation that all subsequent phases build upon. The
project structure follows modular monolith patterns to enable clean extraction
of reusable modules for the MyWork framework brain (SYS-06).

Output: A working Next.js development server with TypeScript, App Router, and
organized directory structure ready for feature development.
</objective>

<execution_context>
@/Users/dansidanutz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dansidanutz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js Application</name>
  <files>

```text
package.json
tsconfig.json
next.config.ts
eslint.config.mjs
src/app/layout.tsx
src/app/page.tsx
src/app/globals.css
public/
.gitignore

```

  </files>
  <action>

```yaml
Run `npx create-next-app@latest . --yes` in the project root to initialize
Next.js with framework defaults.

This command will:

- Create package.json with Next.js 15+, React 19, TypeScript 5.x
- Configure TypeScript with strict mode enabled
- Set up App Router with src/ directory structure
- Configure Turbopack as default bundler
- Set up ESLint with Next.js recommended rules
- Create @/* path alias in tsconfig.json

After initialization, verify the defaults match our requirements:

- Check tsconfig.json has "strict": true
- Check next.config.ts exists (TypeScript config, not .js)
- Check src/app/ directory was created (App Router)

DO NOT customize anything yet - use pure framework defaults for validation.

```html

  </action>
  <verify>

```yaml

1. Run `npm run dev` - server should start on <http://localhost:3000>
2. Run `npm run build` - TypeScript compilation should succeed with no

errors

3. Check `cat tsconfig.json | grep strict` shows "strict": true
4. Check `ls src/app/` shows layout.tsx and page.tsx

```

  </verify>
  <done>

```yaml
Next.js development server starts successfully at localhost:3000, TypeScript
strict mode is enabled, and App Router is configured with src/ directory.

```html

  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Modular Project Structure</name>
  <files>

```text
src/modules/.gitkeep
src/modules/README.md
src/shared/components/.gitkeep
src/shared/lib/.gitkeep
src/shared/types/.gitkeep
src/shared/types/index.ts

```

  </files>
  <action>

```yaml
Create the feature-based modular structure documented in RESEARCH.md:

1. Create directories for business domain modules:

```text

```text

```text

   mkdir -p src/modules

   ```

   Add a README.md explaining the module convention:

   ```markdown

   # Modules

   Each subdirectory is a self-contained business domain module.

   ## Module Structure

```text

```text

```markdown

   module-name/
   ├── components/    # Module-specific UI components
   ├── lib/           # Business logic and server actions
   ├── types/         # TypeScript types
   └── index.ts       # Public API (only export what other modules need)

```markdown

   ## Rules

   - Import from module index only: `@/modules/tasks` not

```
 `@/modules/tasks/lib/internal`

```markdown

   - Each module owns its data and logic
   - Shared code goes in `@/shared/`

```

```yaml

```yaml

2. Create directories for cross-cutting shared code:

```text

   mkdir -p src/shared/components
   mkdir -p src/shared/lib
   mkdir -p src/shared/types

```text

```yaml

```

3. Create base shared types file (src/shared/types/index.ts):

   ```typescript

   // Common types used across modules
   // Add types here as the project grows

   export type ApiResponse<T> = {

```yaml
 success: true
 data: T

```

   } | {

```yaml
 success: false
 error: string

```

   }

```yaml

4. Add .gitkeep files to empty directories to ensure they're tracked:

```text

```text

```text

   touch src/modules/.gitkeep
   touch src/shared/components/.gitkeep
   touch src/shared/lib/.gitkeep

   ```

This structure enables:

- Feature isolation for reusable brain extraction (SYS-06)
- Clear public APIs through index.ts exports
- Separation of business logic from routing

```html

  </action>
  <verify>

```markdown

1. Run `ls -la src/modules/` - should show .gitkeep and README.md
2. Run `ls -la src/shared/` - should show components/, lib/, types/
3. Run `cat src/shared/types/index.ts` - should show ApiResponse type
4. Run `npm run build` - TypeScript should still compile successfully

```html

  </verify>
  <done>

```
Modular directory structure exists with src/modules/ for feature domains and
src/shared/ for cross-cutting concerns, and TypeScript compiles
successfully.

```html

  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the foundation:

1. **Development Server:**

   ```bash
   npm run dev

   # Should start without errors at localhost:3000

```yaml

2. **TypeScript Compilation:**

   ```bash
   npm run build

   # Should complete with no type errors

```yaml

3. **Directory Structure:**

   ```bash
   tree src/ -L 2

   # Should show:

   # src/

   # ├── app/

   # │   ├── globals.css

   # │   ├── layout.tsx

   # │   └── page.tsx

   # ├── modules/

   # │   └── README.md

   # └── shared/

   #     ├── components/

   #     ├── lib/

   #     └── types/

```yaml

4. **Path Aliases:**

   ```bash
   grep -A2 '"paths"' tsconfig.json

   # Should show "@/*": ["./src/*"]

```html

</verification>

<success_criteria>

- Next.js 15+ installed with TypeScript strict mode
- Development server starts without errors
- App Router configured with src/ directory
- Modular structure created (src/modules/, src/shared/)
- Path alias @/* configured and working
- All TypeScript compilation succeeds

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-01-SUMMARY.md`
</output>
