---
phase: 07-performance-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - src/app/(app)/loading.tsx
  - src/app/(app)/dashboard/loading.tsx
  - src/app/(app)/tasks/loading.tsx
  - src/app/(app)/tasks/[id]/edit/loading.tsx
  - src/app/(app)/tasks/new/loading.tsx
  - src/app/(app)/settings/loading.tsx
  - src/shared/components/skeletons/TaskCardSkeleton.tsx
  - src/shared/components/skeletons/TaskListSkeleton.tsx
  - src/shared/components/skeletons/index.ts

autonomous: true

must_haves:
  truths:

```
- "Page transitions show skeleton loading state immediately on navigation"
- "Skeleton screens match the actual layout structure"
- "No blank screens appear during page loads"
- "Loading states provide visual continuity"

```
  artifacts:

```
- path: "src/app/(app)/tasks/loading.tsx"

  provides: "Tasks page loading skeleton"
  min_lines: 30

- path: "src/app/(app)/dashboard/loading.tsx"

  provides: "Dashboard loading skeleton"
  min_lines: 20

- path: "src/shared/components/skeletons/TaskCardSkeleton.tsx"

  provides: "Reusable task card skeleton"
  exports: ["TaskCardSkeleton"]

```
  key_links:

```
- from: "src/app/(app)/tasks/loading.tsx"

  to: "src/shared/components/skeletons/TaskCardSkeleton.tsx"
  via: "import"
  pattern: "import.*TaskCardSkeleton"

```
---

<objective>
Create route-level loading.tsx files with skeleton screens for all main routes

Purpose: Provide immediate visual feedback during page transitions, eliminating
blank screens and improving perceived performance. Skeleton screens should match
the actual layout structure for seamless transitions.

Output: loading.tsx files for each route group with reusable skeleton components
</objective>

<execution_context>
@/Users/dansidanutz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dansidanutz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-quality/07-CONTEXT.md
@.planning/phases/07-performance-quality/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable skeleton components</name>
  <files>

```
src/shared/components/skeletons/TaskCardSkeleton.tsx
src/shared/components/skeletons/TaskListSkeleton.tsx
src/shared/components/skeletons/index.ts

```
  </files>
  <action>
Create skeleton components matching the actual UI structure:

1. `TaskCardSkeleton.tsx`:
   - Match TaskCard dimensions (status badge, title, description, tags area,

```
 actions)

```
   - Use `animate-pulse` for subtle animation
   - Include space for tag badges and file count indicator
   - Match dark mode styling with proper bg-gray-200/dark:bg-gray-700

2. `TaskListSkeleton.tsx`:
   - Render 3 sections (To Do, In Progress, Done) like TaskList
   - Each section shows 3 TaskCardSkeleton components in grid
   - Match grid layout: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`

3. `index.ts`:
   - Export all skeleton components

Pattern from research:

```tsx

export function TaskCardSkeleton() {
  return (

```
<div className="bg-white dark:bg-gray-800 shadow rounded-lg p-4">
  {/* Status badge skeleton */}
  <div className="flex items-center justify-between mb-3">
    <div className="h-6 w-20 bg-gray-200 dark:bg-gray-700 rounded-full animate-pulse" />
  </div>
  {/* Title skeleton */}
  <div className="h-6 w-3/4 bg-gray-200 dark:bg-gray-700 rounded animate-pulse mb-2" />
  {/* Tags skeleton */}
  <div className="flex gap-1 mb-2">
    <div className="h-5 w-14 bg-gray-200 dark:bg-gray-700 rounded animate-pulse" />
    <div className="h-5 w-16 bg-gray-200 dark:bg-gray-700 rounded animate-pulse" />
  </div>
  {/* Description skeleton */}
  <div className="space-y-2 mb-3">
    <div className="h-4 w-full bg-gray-200 dark:bg-gray-700 rounded animate-pulse" />
    <div className="h-4 w-5/6 bg-gray-200 dark:bg-gray-700 rounded animate-pulse" />
  </div>
  {/* Actions skeleton */}
  <div className="flex items-center gap-3">
    <div className="h-8 w-24 bg-gray-200 dark:bg-gray-700 rounded animate-pulse" />
    <div className="flex-1" />
    <div className="h-8 w-12 bg-gray-200 dark:bg-gray-700 rounded animate-pulse" />
    <div className="h-8 w-14 bg-gray-200 dark:bg-gray-700 rounded animate-pulse" />
  </div>
</div>

```
  )
}

```html

  </action>
  <verify>
Files exist and export expected components:

- `ls src/shared/components/skeletons/`
- TypeScript compiles without errors: `npx tsc --noEmit`

  </verify>
  <done>
Reusable skeleton components exist in src/shared/components/skeletons/ with
TaskCardSkeleton, TaskListSkeleton, and index.ts exporting them
  </done>
</task>

<task type="auto">
  <name>Task 2: Create route-level loading.tsx files</name>
  <files>

```
src/app/(app)/loading.tsx
src/app/(app)/dashboard/loading.tsx
src/app/(app)/tasks/loading.tsx
src/app/(app)/tasks/[id]/edit/loading.tsx
src/app/(app)/tasks/new/loading.tsx
src/app/(app)/settings/loading.tsx
src/app/(app)/settings/profile/loading.tsx

```
  </files>
  <action>
Create loading.tsx files for each route that matches the page layout:

1. `src/app/(app)/loading.tsx` - Generic app shell loading
   - Show header placeholder (already rendered by layout)
   - Main content area with centered spinner or skeleton

2. `src/app/(app)/dashboard/loading.tsx`:
   - Header skeleton (title + button)
   - 3-column stats grid with number placeholders
   - Getting started/quick actions section skeleton

3. `src/app/(app)/tasks/loading.tsx`:
   - Use the existing TasksLoadingSkeleton pattern from tasks/page.tsx
   - Include search bar, filter sidebar, and task list skeletons

4. `src/app/(app)/tasks/[id]/edit/loading.tsx`:
   - Breadcrumb skeleton
   - Form fields skeleton (title, description, status, tags, files)

5. `src/app/(app)/tasks/new/loading.tsx`:
   - Similar to edit but without existing data placeholders

6. `src/app/(app)/settings/loading.tsx`:
   - Settings navigation skeleton
   - Content area skeleton

7. `src/app/(app)/settings/profile/loading.tsx`:
   - Profile form skeleton (avatar, name, bio fields)

All loading files should:

- NOT use 'use client' (Server Components by default)
- Export default function Loading()
- Use Tailwind animate-pulse for animations

  </action>
  <verify>
All loading files exist and render properly:

- `ls src/app/(app)/*/loading.tsx src/app/(app)/loading.tsx`
- Start dev server and navigate between routes to see skeletons: `npm run dev`
- TypeScript compiles: `npx tsc --noEmit`

  </verify>
  <done>
Loading.tsx files exist for all main routes and show skeleton screens during
navigation
  </done>
</task>

</tasks>

<verification>

1. Navigate between routes - skeleton screens appear immediately
2. TypeScript compilation succeeds
3. Skeleton layouts match actual page layouts
4. No blank screens during page transitions

</verification>

<success_criteria>

- All main routes have loading.tsx files
- Skeleton screens match actual page layouts
- Page transitions show immediate loading feedback
- No "flash of nothing" during navigation

</success_criteria>

<output>
After completion, create
`.planning/phases/07-performance-quality/07-01-SUMMARY.md`
</output>
