---
phase: 02-authentication-profiles
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/page.tsx
  - src/app/(auth)/welcome/page.tsx
  - src/app/(auth)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "User can see Login with GitHub button on /login page"
    - "User can see Login with GitHub CTA on homepage hero"
    - "User sees welcome/onboarding page after first OAuth login"
    - "OAuth errors display on login page with retry option"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page with GitHub OAuth button"
      min_lines: 30
    - path: "src/app/page.tsx"
      provides: "Homepage with hero CTA for login"
      min_lines: 20
    - path: "src/app/(auth)/welcome/page.tsx"
      provides: "Post-OAuth onboarding page"
      min_lines: 25
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/shared/lib/auth.ts"
      via: "imports signIn"
      pattern: "import.*signIn.*from.*auth"
    - from: "src/app/(auth)/login/page.tsx"
      to: "/api/auth/signin/github"
      via: "form action or signIn call"
      pattern: "signIn.*github|action.*signin.*github"
---

<objective>
Create login page with GitHub OAuth button, update homepage with prominent login CTA, and create welcome/onboarding page for post-OAuth flow.

Purpose: Provide the user-facing authentication entry points. Per CONTEXT.md: login button on homepage hero, post-OAuth redirect to welcome page, OAuth errors redirect back to login with error message.

Output: Login page at /login, updated homepage with hero CTA, welcome page at /welcome.
</objective>

<execution_context>
@/Users/dansidanutz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dansidanutz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-profiles/02-CONTEXT.md
@.planning/phases/02-authentication-profiles/02-RESEARCH.md

# Dependencies from 02-01
@src/shared/lib/auth.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Login Page with GitHub OAuth</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/layout.tsx</files>
  <action>
1. Create auth route group layout at src/app/(auth)/layout.tsx:

```typescript
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50 dark:bg-gray-900">
      <div className="w-full max-w-md px-4">
        {children}
      </div>
    </div>
  )
}
```

2. Create login page at src/app/(auth)/login/page.tsx:

```typescript
import { signIn } from '@/shared/lib/auth'
import { redirect } from 'next/navigation'
import { auth } from '@/shared/lib/auth'

interface LoginPageProps {
  searchParams: Promise<{
    callbackUrl?: string
    error?: string
  }>
}

export default async function LoginPage({ searchParams }: LoginPageProps) {
  // Check if already authenticated
  const session = await auth()
  if (session?.user) {
    redirect('/dashboard')
  }

  const params = await searchParams
  const callbackUrl = params.callbackUrl || '/dashboard'
  const error = params.error

  return (
    <div className="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-8">
      <div className="text-center mb-8">
        <h1 className="text-2xl font-bold text-gray-900 dark:text-white mb-2">
          Welcome to Task Tracker
        </h1>
        <p className="text-gray-600 dark:text-gray-400">
          Sign in to manage your tasks and stay organized
        </p>
      </div>

      {error && (
        <div className="mb-6 p-4 bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-lg">
          <p className="text-sm text-red-600 dark:text-red-400">
            {error === 'OAuthAccountNotLinked'
              ? 'This email is already associated with another account.'
              : error === 'OAuthCallback'
              ? 'There was a problem signing in. Please try again.'
              : 'An error occurred during sign in. Please try again.'}
          </p>
        </div>
      )}

      <form
        action={async () => {
          'use server'
          await signIn('github', { redirectTo: callbackUrl })
        }}
      >
        <button
          type="submit"
          className="w-full flex items-center justify-center gap-3 px-4 py-3 bg-gray-900 dark:bg-white text-white dark:text-gray-900 rounded-lg font-medium hover:bg-gray-800 dark:hover:bg-gray-100 transition-colors"
        >
          <svg className="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          Sign in with GitHub
        </button>
      </form>

      <div className="mt-6 text-center">
        <a
          href="/"
          className="text-sm text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white"
        >
          &larr; Back to home
        </a>
      </div>
    </div>
  )
}
```

Create directory structure:
```bash
mkdir -p src/app/\(auth\)/login
```

IMPORTANT:
- Use Server Action for signIn (form action with 'use server')
- Handle error param from URL for OAuth failures (per CONTEXT.md)
- Check if already authenticated and redirect to dashboard
  </action>
  <verify>
- File exists at src/app/(auth)/login/page.tsx
- File exists at src/app/(auth)/layout.tsx
- `npm run build` succeeds
- `npm run dev` then visit http://localhost:3000/login shows login page
  </verify>
  <done>
Login page created with GitHub OAuth button and error handling. Auth layout provides centered card design.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Homepage with Hero CTA</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the existing homepage with a proper landing page featuring GitHub login CTA in the hero section.

```typescript
import Link from 'next/link'
import { auth } from '@/shared/lib/auth'

export default async function HomePage() {
  const session = await auth()

  return (
    <main className="min-h-screen bg-gradient-to-b from-gray-50 to-white dark:from-gray-900 dark:to-gray-800">
      {/* Navigation */}
      <nav className="container mx-auto px-4 py-6 flex justify-between items-center">
        <div className="text-xl font-bold text-gray-900 dark:text-white">
          Task Tracker
        </div>
        {session?.user ? (
          <Link
            href="/dashboard"
            className="px-4 py-2 bg-gray-900 dark:bg-white text-white dark:text-gray-900 rounded-lg font-medium hover:bg-gray-800 dark:hover:bg-gray-100 transition-colors"
          >
            Go to Dashboard
          </Link>
        ) : (
          <Link
            href="/login"
            className="px-4 py-2 text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white"
          >
            Sign in
          </Link>
        )}
      </nav>

      {/* Hero Section */}
      <section className="container mx-auto px-4 py-20 text-center">
        <h1 className="text-5xl font-bold text-gray-900 dark:text-white mb-6">
          Organize Your Tasks,
          <br />
          <span className="text-blue-600 dark:text-blue-400">Ship Faster</span>
        </h1>
        <p className="text-xl text-gray-600 dark:text-gray-400 mb-8 max-w-2xl mx-auto">
          A simple, powerful task tracker that helps you stay focused and get things done.
          Connect with GitHub to sync your workflow.
        </p>

        {session?.user ? (
          <Link
            href="/dashboard"
            className="inline-flex items-center gap-2 px-8 py-4 bg-blue-600 text-white rounded-lg font-semibold text-lg hover:bg-blue-700 transition-colors"
          >
            Open Dashboard
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </Link>
        ) : (
          <Link
            href="/login"
            className="inline-flex items-center gap-3 px-8 py-4 bg-gray-900 dark:bg-white text-white dark:text-gray-900 rounded-lg font-semibold text-lg hover:bg-gray-800 dark:hover:bg-gray-100 transition-colors"
          >
            <svg className="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
            Login with GitHub
          </Link>
        )}
      </section>

      {/* Features Section - Preview for anonymous users (per CONTEXT.md) */}
      <section className="container mx-auto px-4 py-16">
        <div className="grid md:grid-cols-3 gap-8">
          <div className="p-6 bg-white dark:bg-gray-800 rounded-lg shadow-sm">
            <div className="w-12 h-12 bg-blue-100 dark:bg-blue-900 rounded-lg flex items-center justify-center mb-4">
              <svg className="w-6 h-6 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4" />
              </svg>
            </div>
            <h3 className="text-lg font-semibold text-gray-900 dark:text-white mb-2">
              Simple Task Management
            </h3>
            <p className="text-gray-600 dark:text-gray-400">
              Create, organize, and track your tasks with an intuitive interface.
            </p>
          </div>

          <div className="p-6 bg-white dark:bg-gray-800 rounded-lg shadow-sm">
            <div className="w-12 h-12 bg-green-100 dark:bg-green-900 rounded-lg flex items-center justify-center mb-4">
              <svg className="w-6 h-6 text-green-600 dark:text-green-400" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
              </svg>
            </div>
            <h3 className="text-lg font-semibold text-gray-900 dark:text-white mb-2">
              GitHub Integration
            </h3>
            <p className="text-gray-600 dark:text-gray-400">
              Connect your GitHub account for seamless workflow integration.
            </p>
          </div>

          <div className="p-6 bg-white dark:bg-gray-800 rounded-lg shadow-sm">
            <div className="w-12 h-12 bg-purple-100 dark:bg-purple-900 rounded-lg flex items-center justify-center mb-4">
              <svg className="w-6 h-6 text-purple-600 dark:text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01" />
              </svg>
            </div>
            <h3 className="text-lg font-semibold text-gray-900 dark:text-white mb-2">
              File Attachments
            </h3>
            <p className="text-gray-600 dark:text-gray-400">
              Attach files to tasks with drag-and-drop support.
            </p>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="container mx-auto px-4 py-8 text-center text-gray-600 dark:text-gray-400">
        <p>Built with Next.js and love</p>
      </footer>
    </main>
  )
}
```

This homepage:
- Shows "Login with GitHub" CTA in hero for anonymous users (per CONTEXT.md)
- Shows "Go to Dashboard" for authenticated users
- Provides preview of features to encourage sign-up (per CONTEXT.md: "Limited demo access")
  </action>
  <verify>
- File exists at src/app/page.tsx
- `npm run build` succeeds
- `npm run dev` then visit http://localhost:3000 shows hero with GitHub login CTA
  </verify>
  <done>
Homepage updated with hero CTA for GitHub login. Shows different content for auth/unauth users.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Welcome/Onboarding Page</name>
  <files>src/app/(auth)/welcome/page.tsx</files>
  <action>
Create welcome page at src/app/(auth)/welcome/page.tsx for post-OAuth onboarding:

```typescript
import { redirect } from 'next/navigation'
import Link from 'next/link'
import { getUser } from '@/shared/lib/dal'

export default async function WelcomePage() {
  // This page requires authentication
  const user = await getUser()

  // If somehow not authenticated, redirect to login
  if (!user) {
    redirect('/login')
  }

  return (
    <div className="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-8">
      <div className="text-center mb-8">
        {/* User avatar */}
        {user.image && (
          <img
            src={user.image}
            alt={user.name || 'User avatar'}
            className="w-20 h-20 rounded-full mx-auto mb-4 border-4 border-blue-100 dark:border-blue-900"
          />
        )}

        <h1 className="text-2xl font-bold text-gray-900 dark:text-white mb-2">
          Welcome, {user.name || 'there'}!
        </h1>
        <p className="text-gray-600 dark:text-gray-400">
          Your account is all set up. Let&apos;s get you started.
        </p>
      </div>

      {/* Quick tour / onboarding steps */}
      <div className="space-y-4 mb-8">
        <div className="flex items-start gap-4 p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
          <div className="w-8 h-8 bg-blue-100 dark:bg-blue-900 rounded-full flex items-center justify-center flex-shrink-0">
            <span className="text-blue-600 dark:text-blue-400 font-semibold">1</span>
          </div>
          <div>
            <h3 className="font-medium text-gray-900 dark:text-white">Create your first task</h3>
            <p className="text-sm text-gray-600 dark:text-gray-400">
              Click the &quot;New Task&quot; button to add your first item
            </p>
          </div>
        </div>

        <div className="flex items-start gap-4 p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
          <div className="w-8 h-8 bg-blue-100 dark:bg-blue-900 rounded-full flex items-center justify-center flex-shrink-0">
            <span className="text-blue-600 dark:text-blue-400 font-semibold">2</span>
          </div>
          <div>
            <h3 className="font-medium text-gray-900 dark:text-white">Organize with categories</h3>
            <p className="text-sm text-gray-600 dark:text-gray-400">
              Group related tasks together for better organization
            </p>
          </div>
        </div>

        <div className="flex items-start gap-4 p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
          <div className="w-8 h-8 bg-blue-100 dark:bg-blue-900 rounded-full flex items-center justify-center flex-shrink-0">
            <span className="text-blue-600 dark:text-blue-400 font-semibold">3</span>
          </div>
          <div>
            <h3 className="font-medium text-gray-900 dark:text-white">Track your progress</h3>
            <p className="text-sm text-gray-600 dark:text-gray-400">
              Mark tasks as done and watch your productivity soar
            </p>
          </div>
        </div>
      </div>

      {/* CTA to continue */}
      <div className="space-y-3">
        <Link
          href="/dashboard"
          className="block w-full text-center px-4 py-3 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 transition-colors"
        >
          Go to Dashboard
        </Link>
        <Link
          href="/settings/profile"
          className="block w-full text-center px-4 py-3 border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 rounded-lg font-medium hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors"
        >
          Set up your profile
        </Link>
      </div>
    </div>
  )
}
```

Create directory:
```bash
mkdir -p src/app/\(auth\)/welcome
```

This page:
- Shows user avatar and name from GitHub
- Provides quick onboarding steps
- Links to dashboard and profile setup
- Uses getUser from DAL for auth check
  </action>
  <verify>
- File exists at src/app/(auth)/welcome/page.tsx
- `npm run build` succeeds
- Page uses getUser from DAL for authentication
  </verify>
  <done>
Welcome page created with onboarding flow. Shows user info from GitHub and provides next steps.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Build verification:
   - `npm run build` completes successfully

2. Route verification:
   - /login - Shows login page with GitHub button
   - / - Shows homepage with hero CTA
   - /welcome - Protected, requires auth (redirects if not logged in)

3. UI verification:
   - Login page displays GitHub OAuth button
   - Homepage displays "Login with GitHub" CTA in hero
   - Error handling on login page (add ?error=test to URL)
</verification>

<success_criteria>
- [ ] Login page exists at /login with GitHub OAuth button
- [ ] Homepage has hero section with "Login with GitHub" CTA
- [ ] Welcome page exists at /welcome with user greeting
- [ ] Auth layout provides centered card design
- [ ] Error param displays error message on login page
- [ ] Build succeeds without errors
- [ ] All pages use proper dark mode support
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-profiles/02-03-SUMMARY.md`
</output>
